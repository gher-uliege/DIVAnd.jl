# execute from root of project, e.g. $HOME/Julia/DIVAnd.jl
# remove trailing space
perl -pi -e  's/\S$//g' src/*jl test/*jl
# expand tab by space
perl -pi -e  's/\t/     /g' src/*jl test/*jl
julia --eval 'using JuliaFormatter; format(["src","test"])'